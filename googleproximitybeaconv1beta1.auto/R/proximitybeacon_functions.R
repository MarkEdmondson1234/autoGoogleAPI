#' Google Proximity Beacon API
#' Registers, manages, indexes, and searches beacons.
#' 
#' Auto-generated code by googleAuthR::gar_create_api_skeleton
#'  at 2017-03-05 19:58:07
#' filename: /Users/mark/dev/R/autoGoogleAPI/googleproximitybeaconv1beta1.auto/R/proximitybeacon_functions.R
#' api_json: api_json
#' 
#' @details 
#' Authentication scopes used are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' @docType package 
#' @name proximitybeacon_googleAuthR
#' 
NULL
## NULL

#' A helper function that tests whether an object is either NULL _or_
#' a list of NULLs
#'
#' @keywords internal
is.NullOb <- function(x) is.null(x) | all(sapply(x, is.null))
#' Recursively step down into list, removing all such objects
#'
#' @keywords internal
rmNullObs <- function(x) {
    x <- Filter(Negate(is.NullOb), x)
    lapply(x, function(x) if (is.list(x)) 
        rmNullObs(x) else x)
}

#' Registers a previously unregistered beacon given its `advertisedId`. These IDs are unique within the system. An ID can be registered only once. Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param Beacon The \link{Beacon} object to pass to this method
#' @param projectId The project id of the project the beacon will be registered to
#' @importFrom googleAuthR gar_api_generator
#' @family Beacon functions
#' @export
beacons.register <- function(Beacon, projectId = NULL) {
    url <- "https://proximitybeacon.googleapis.com/v1beta1/beacons:register"
    # proximitybeacon.beacons.register
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "POST", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    stopifnot(inherits(Beacon, "gar_Beacon"))
    
    f(the_body = Beacon)
    
}

#' Decommissions the specified beacon in the service. This beacon will no longer be returned from `beaconinfo.getforobserved`. This operation is permanent -- you will not be able to re-register a beacon with this ID again. Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param beaconName Beacon that should be decommissioned
#' @param projectId The project id of the beacon to decommission
#' @importFrom googleAuthR gar_api_generator
#' @export
beacons.decommission <- function(beaconName, projectId = NULL) {
    url <- sprintf("https://proximitybeacon.googleapis.com/v1beta1/{+beaconName}:decommission", 
        beaconName)
    # proximitybeacon.beacons.decommission
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "POST", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    f()
    
}

#' Returns detailed information about the specified beacon. Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **viewer**, **Is owner** or **Can edit** permissions in the Google Developers Console project. Requests may supply an Eddystone-EID beacon name in the form: `beacons/4!beaconId` where the `beaconId` is the base16 ephemeral ID broadcast by the beacon. The returned `Beacon` object will contain the beacon's stable Eddystone-UID. Clients not authorized to resolve the beacon's ephemeral Eddystone-EID broadcast will receive an error.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param beaconName Resource name of this beacon
#' @param projectId The project id of the beacon to request
#' @importFrom googleAuthR gar_api_generator
#' @export
beacons.get <- function(beaconName, projectId = NULL) {
    url <- sprintf("https://proximitybeacon.googleapis.com/v1beta1/{+beaconName}", 
        beaconName)
    # proximitybeacon.beacons.get
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "GET", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    f()
    
}

#' Searches the beacon registry for beacons that match the given search criteria. Only those beacons that the client has permission to list will be returned. Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **viewer**, **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param q Filter query string that supports the following field filters: * `description:''` For example: `description:'Room 3'` Returns beacons whose description matches tokens in the string 'Room 3' (not necessarily that exact string)
#' @param pageToken A pagination token obtained from a previous request to list beacons
#' @param pageSize The maximum number of records to return for this request, up to a server-defined upper limit
#' @param projectId The project id to list beacons under
#' @importFrom googleAuthR gar_api_generator
#' @export
beacons.list <- function(q = NULL, pageToken = NULL, pageSize = NULL, projectId = NULL) {
    url <- "https://proximitybeacon.googleapis.com/v1beta1/beacons"
    # proximitybeacon.beacons.list
    pars = list(q = q, pageToken = pageToken, pageSize = pageSize, projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "GET", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    f()
    
}

#' Updates the information about the specified beacon. **Any field that you do not populate in the submitted beacon will be permanently erased**, so you should follow the 'read, modify, write' pattern to avoid inadvertently destroying data. Changes to the beacon status via this method will be silently ignored. To update beacon status, use the separate methods on this API for activation, deactivation, and decommissioning. Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param Beacon The \link{Beacon} object to pass to this method
#' @param beaconName Resource name of this beacon
#' @param projectId The project id of the beacon to update
#' @importFrom googleAuthR gar_api_generator
#' @family Beacon functions
#' @export
beacons.update <- function(Beacon, beaconName, projectId = NULL) {
    url <- sprintf("https://proximitybeacon.googleapis.com/v1beta1/{+beaconName}", 
        beaconName)
    # proximitybeacon.beacons.update
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "PUT", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    stopifnot(inherits(Beacon, "gar_Beacon"))
    
    f(the_body = Beacon)
    
}

#' Activates a beacon. A beacon that is active will return information and attachment data when queried via `beaconinfo.getforobserved`. Calling this method on an already active beacon will do nothing (but will return a successful response code). Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param beaconName Beacon that should be activated
#' @param projectId The project id of the beacon to activate
#' @importFrom googleAuthR gar_api_generator
#' @export
beacons.activate <- function(beaconName, projectId = NULL) {
    url <- sprintf("https://proximitybeacon.googleapis.com/v1beta1/{+beaconName}:activate", 
        beaconName)
    # proximitybeacon.beacons.activate
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "POST", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    f()
    
}

#' Deactivates a beacon. Once deactivated, the API will not return information nor attachment data for the beacon when queried via `beaconinfo.getforobserved`. Calling this method on an already inactive beacon will do nothing (but will return a successful response code). Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param beaconName Beacon that should be deactivated
#' @param projectId The project id of the beacon to deactivate
#' @importFrom googleAuthR gar_api_generator
#' @export
beacons.deactivate <- function(beaconName, projectId = NULL) {
    url <- sprintf("https://proximitybeacon.googleapis.com/v1beta1/{+beaconName}:deactivate", 
        beaconName)
    # proximitybeacon.beacons.deactivate
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "POST", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    f()
    
}

#' Lists all attachment namespaces owned by your Google Developers Console project. Attachment data associated with a beacon must include a namespaced type, and the namespace must be owned by your project. Authenticate using an [OAuth access token](https://developers.google.com/identity/protocols/OAuth2) from a signed-in user with **viewer**, **Is owner** or **Can edit** permissions in the Google Developers Console project.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param projectId The project id to list namespaces under
#' @importFrom googleAuthR gar_api_generator
#' @export
namespaces.list <- function(projectId = NULL) {
    url <- "https://proximitybeacon.googleapis.com/v1beta1/namespaces"
    # proximitybeacon.namespaces.list
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "GET", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    f()
    
}

#' Updates the information about the specified namespace. Only the namespace visibility can be updated.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param Namespace The \link{Namespace} object to pass to this method
#' @param namespaceName Resource name of this namespace
#' @param projectId The project id of the namespace to update
#' @importFrom googleAuthR gar_api_generator
#' @family Namespace functions
#' @export
namespaces.update <- function(Namespace, namespaceName, projectId = NULL) {
    url <- sprintf("https://proximitybeacon.googleapis.com/v1beta1/{+namespaceName}", 
        namespaceName)
    # proximitybeacon.namespaces.update
    pars = list(projectId = projectId)
    f <- googleAuthR::gar_api_generator(url, "PUT", pars_args = rmNullObs(pars), 
        data_parse_function = function(x) x)
    stopifnot(inherits(Namespace, "gar_Namespace"))
    
    f(the_body = Namespace)
    
}

#' Gets the Proximity Beacon API's current public key and associated parameters used to initiate the Diffie-Hellman key exchange required to register a beacon that broadcasts the Eddystone-EID format. This key changes periodically; clients may cache it and re-use the same public key to provision and register multiple beacons. However, clients should be prepared to refresh this key when they encounter an error registering an Eddystone-EID beacon.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item https://www.googleapis.com/auth/userlocation.beacon.registry
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c(https://www.googleapis.com/auth/userlocation.beacon.registry)}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' #' @importFrom googleAuthR gar_api_generator
#' @export
getEidparams <- function() {
    url <- "https://proximitybeacon.googleapis.com/v1beta1/eidparams"
    # proximitybeacon.getEidparams
    f <- googleAuthR::gar_api_generator(url, "GET", data_parse_function = function(x) x)
    f()
    
}


#' Given one or more beacon observations, returns any beacon information and attachments accessible to your application. Authorize by using the [API key](https://developers.google.com/beacons/proximity/how-tos/authorizing#APIKey) for the application.
#' 
#' Autogenerated via \code{\link[googleAuthR]{gar_create_api_skeleton}}
#' 
#' @seealso \href{https://developers.google.com/beacons/proximity/}{Google Documentation}
#' 
#' @details 
#' Authentication scopes used by this function are:
#' \itemize{
#'   \item 
#' }
#' 
#' Set \code{options(googleAuthR.scopes.selected = c()}
#' Then run \code{googleAuthR::gar_auth()} to authenticate.
#' See \code{\link[googleAuthR]{gar_auth}} for details. 
#' 
#' @param GetInfoForObservedBeaconsRequest The \link{GetInfoForObservedBeaconsRequest} object to pass to this method
#' #' @importFrom googleAuthR gar_api_generator
#' @family GetInfoForObservedBeaconsRequest functions
#' @export


beaconinfo.getforobserved <- function(GetInfoForObservedBeaconsRequest) {
    
    
    url <- "https://proximitybeacon.googleapis.com/v1beta1/beaconinfo:getforobserved"
    # proximitybeacon.beaconinfo.getforobserved
    
    f <- googleAuthR::gar_api_generator(url, "POST", data_parse_function = function(x) x)
    
    stopifnot(inherits(GetInfoForObservedBeaconsRequest, "gar_GetInfoForObservedBeaconsRequest"))
    
    f(the_body = GetInfoForObservedBeaconsRequest)
    
    
}



